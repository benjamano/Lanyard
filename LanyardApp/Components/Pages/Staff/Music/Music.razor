@page "/staff/music"

@attribute [Authorize]

@using Lanyard.App.Components.Music
@using Lanyard.Application.Services
@using Lanyard.Application.Services.Authentication
@using Lanyard.Infrastructure.DTO
@using Lanyard.Infrastructure.Models
@using Microsoft.AspNetCore.Authorization
@using NAudio.Wave;

@inject MusicPlayerService _player
@inject IClientService _clientService
@inject IToastService _toastService

@implements IDisposable

<PageTitle>Music</PageTitle>

<FluentCard Class="mb-2 p-2">
    <FluentStack Orientation="Orientation.Horizontal" HorizontalAlignment="HorizontalAlignment.Right" VerticalAlignment="VerticalAlignment.Center" HorizontalGap="12">
        <FluentSelect
                      @bind-Value="selectedClientIdString"
                      TOption="string"
                      Class="col-auto"
                      Disabled="@(!(clients.Count() > 1))">

            @if (!clients.Any())
            {
                <FluentOption Value="">No connected clients</FluentOption>
            }
            else
            {
                @foreach (ClientConnectedWithCapabilitiesDTO client in clients.OrderBy(x => x.Name))
                {
                    <FluentOption Value="@client.Id.ToString()">@client.Name</FluentOption>
                }
            }
        </FluentSelect>

        <FluentButton Appearance="Appearance.Outline" OnClick="RefreshClientsAsync">
            Refresh
        </FluentButton>
    </FluentStack>
</FluentCard>

<div style="height: calc(100vh - 120px); overflow-y: auto;">
    <FluentStack Orientation="Orientation.Horizontal" Style="height: 100%;">
        <Playlists OnSongsSelected="HandleSongsSelected" SelectedClientId="selectedClientId"></Playlists>

        <FluentStack Orientation="Orientation.Vertical" Style="width: 100%; padding-left: 0.5rem;">
            <SongList Songs="currentSongs" SelectedClientId="selectedClientId"></SongList>
        </FluentStack>
    </FluentStack>
</div>

<FluentStack Orientation="Orientation.Horizontal"
             HorizontalAlignment="HorizontalAlignment.Center"
             VerticalAlignment="VerticalAlignment.Center"
             Style="position: fixed; bottom: 0; left: 0; right: 0; width: 100%; padding: 1rem; background-color: var(--neutral-layer-1); border-top: 1px solid var(--neutral-stroke-divider-rest); z-index: 1000; box-shadow: 0 -2px 8px rgba(0,0,0,0.1);">
    <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8" HorizontalAlignment="HorizontalAlignment.Center" VerticalAlignment="VerticalAlignment.Center">
        <FluentButton Appearance="Appearance.Outline"
                      IconOnly="true"
                      Title="Previous (Double-click) / Restart (Single-click)"
                      OnClick="RestartSong"
                      @ondblclick="PreviousSong"
                      Disabled="@(selectedClientId == null)"
                      Style="border-radius: 50%; width: 40px; height: 40px;">
            <FluentIcon Value="@(new Icons.Regular.Size20.Previous())" />
        </FluentButton>

        <FluentButton Appearance="Appearance.Accent"
                      IconOnly="true"
                      Title="@(isPlaying ? "Pause" : "Play")"
                      OnClick="TogglePlay"
                      Disabled="@(selectedClientId == null)"
                      Style="border-radius: 50%; width: 40px; height: 40px;">
            @if (isPlaying)
            {
                <FluentIcon Color="Color.Fill" Icon="Icons.Regular.Size20.Pause" />
            }
            else
            {
                <FluentIcon Color="Color.Fill" Icon="Icons.Regular.Size20.Play" />
            }
        </FluentButton>

        <FluentButton Appearance="Appearance.Outline"
                      IconOnly="true"
                      Title="Next"
                      OnClick="NextSong"
                      Disabled="@(selectedClientId == null)"
                      Style="border-radius: 50%; width: 40px; height: 40px;">
            <FluentIcon Value="@(new Icons.Regular.Size20.Next())" />
        </FluentButton>
    </FluentStack>
</FluentStack>

@code {
    private IEnumerable<PlaylistSongMember>? currentSongs;
    private bool isPlaying = false;
    private List<ClientConnectedWithCapabilitiesDTO> clients = [];
    private Guid? selectedClientId;

    private string selectedClientIdString
    {
        get => selectedClientId?.ToString() ?? string.Empty;
        set
        {
            if (Guid.TryParse(value, out Guid clientId))
            {
                selectedClientId = clientId;
                isPlaying = _player.GetCurrentPlaybackState(clientId) == PlaybackState.Playing;
            }
            else
            {
                selectedClientId = null;
                isPlaying = false;
            }
        }
    }

    protected override async Task OnInitializedAsync()
    {
        _player.OnPlaybackStatusChanged += HandlePlaybackStateChanged;
        await RefreshClientsAsync();
    }

    private void HandlePlaybackStateChanged(Guid clientId, PlaybackState state)
    {
        if (selectedClientId != clientId)
        {
            return;
        }

        isPlaying = state == PlaybackState.Playing;
        InvokeAsync(StateHasChanged);
    }

    private async Task RefreshClientsAsync()
    {
        Result<IEnumerable<ClientConnectedWithCapabilitiesDTO>> result = await _clientService.GetClientsWithCapabilitiesAsync();

        if (!result.IsSuccess || result.Data == null)
        {
            _toastService.ShowError($"Failed to load clients: {result.Error}");
            clients = [];
            selectedClientId = null;
            isPlaying = false;
            return;
        }

        clients = result.Data
            .Where(x => x.IsCurrentlyConnected)
            .OrderBy(x => x.Name)
            .ToList();

        if (!clients.Any())
        {
            selectedClientId = null;
            isPlaying = false;
            return;
        }

        if (selectedClientId == null || !clients.Any(x => x.Id == selectedClientId))
        {
            selectedClientId = clients.First().Id;
        }

        isPlaying = _player.GetCurrentPlaybackState(selectedClientId.Value) == PlaybackState.Playing;
    }

    private async Task TogglePlay()
    {
        if (selectedClientId == null)
        {
            _toastService.ShowWarning("Select a connected client first.");
            return;
        }

        await _player.TogglePlay(selectedClientId.Value);
    }

    private async Task RestartSong()
    {
        if (selectedClientId == null)
        {
            _toastService.ShowWarning("Select a connected client first.");
            return;
        }

        await _player.Restart(selectedClientId.Value);
    }

    private async Task PreviousSong()
    {
        if (selectedClientId == null)
        {
            _toastService.ShowWarning("Select a connected client first.");
            return;
        }

        await _player.Previous(selectedClientId.Value);
    }

    private async Task NextSong()
    {
        if (selectedClientId == null)
        {
            _toastService.ShowWarning("Select a connected client first.");
            return;
        }

        await _player.Next(selectedClientId.Value);
    }

    private void HandleSongsSelected(IEnumerable<PlaylistSongMember> songs)
    {
        currentSongs = songs;
        StateHasChanged();
    }

    public void Dispose()
    {
        _player.OnPlaybackStatusChanged -= HandlePlaybackStateChanged;
    }
}
